# Internal docs — do not publish

This directory holds internal working documents. The repository root publishes to
mindsovermatters.co via GitHub Pages, so **files merged to `main` become publicly
readable at that domain**. Keep internal analysis on feature branches, or exclude this
directory from the published site before merging anything here.
